# from django.urls import re_path

# from . import views

# # pylint: disable=invalid-name
# urlpatterns = [
#     re_path(r'^sslcommerz/$', views.CreatePayment.as_view(),
#         name="ssl_commerz.create-payment"),
#     re_path(r'^sslcommerz/ipn/$', views.ValidateIpnOrder.as_view(),
#         name="ssl_commerz.ipn-validate"),
#     re_path(r'^sslcommerz/payments/$', views.PaymentList.as_view(),
#         name="ssl_commerz.payment-list")
# ]
